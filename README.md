# RoadMapDesarrollo
Road map de desarrollo para hacer proyectos con buenas practicas salir con conocimientos y fundamentos fuertes 

🧭 ROADMAP DE DESARROLLO (actualizado)
✅ Fase 1 – Fundamentos de Frontend (completada)
  HTML: estructura, formularios, inputs, listas, secciones.
  CSS: box model, márgenes, padding, colores, tipografías, flexbox básico.
  JS básico: variables, funciones, condicionales, bucles, DOM (querySelector, eventos), manejo básico de errores.
✅ Fase 2 – APIs y lógica avanzada en Frontend (completada)
  Fetch API (GET, POST, PUT, DELETE), response.json(), try...catch.
  APIs públicas: OpenWeatherMap, OMDb, JSONPlaceholder.
  Integración en proyectos: clima, buscador de películas, TODOs CRUD simulado.
  Extras: animaciones ligeras, estructura JS más ordenada.
🔄 Fase 3 – React (en progreso)
  Por qué React y creación con Vite.
  JSX, componentes funcionales.
  Props y estado (useState).
  Renderizado de listas y condicionales.
  useEffect (a medida que avancemos), llamadas a API.
  (Opcional) React Router según necesidad.
  Proyecto 1 (Buscador de Películas) – ✅
  Cubierto: JSX, props, useState, eventos, fetch/async-await, manejo de errores, listas/keys, condicionales.
  Buenas prácticas: componentización (SearchBar, MovieList, MovieCard), barrel file, export default/nombrado.
  Funciones: filtro por tipo, grilla responsive, animaciones hover/focus/tap, respeto a prefers-reduced-motion, cards centradas con ancho máximo.
  Proyecto 2 – ⏳ Próximo
  Paginación (parámetro page de OMDb).
  Skeletons de carga.
  Accesibilidad: foco visible, aria-live, navegación por teclado.
  Proyecto 3 – ⏳ Próximo
  Lazy-load en imágenes (loading="lazy").
  Favoritos con localStorage.
  Más práctica de useEffect aplicado.
⏭️ Fase 4 – Backend y Bases de Datos (pendiente)
  Node.js + Express, rutas/controladores.
  Conexión a DB (MongoDB o SQL), CRUD real.
  Autenticación básica (login, tokens).
⏭️ Fase 5 – Herramientas avanzadas y despliegue (pendiente)
  Testing (Jest, React Testing Library, e2e si aplica).
  Optimización de rendimiento.
  Buenas prácticas de estructura.
  Deploy (Netlify, Vercel, Render, etc.), variables de entorno.
  🧰 Git (reubicado: lo trabajaremos a fondo entrando a backend/flujo completo)
  Instalación/configuración, flujo básico (init, status, add, commit, log).
  Ramas y merges; resolución de conflictos.
GitHub: remotos, push, pull, clone.
Buenas prácticas: mensajes de commit claros, .gitignore, branches por feature.
